# env-validator

Validate environment variables against a TypeScript schema and return a typed
configuration object.

Zero runtime dependencies. No `.env` loading — it validates an explicit source
such as `process.env`.

## Installation

```sh
pnpm add env-validator
```

## License

MIT
